<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="ir.map.sdkdemo_map">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:name=".AppController"
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">

        <meta-data
            android:name="com.google.android.maps.v2.API_KEY"
            android:value="AIzaSyAvvR6vDDafdq7F8SbHjKMiLrvRYUtFrMI" />

        <activity android:name=".MainActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity
            android:name=".BasicMapActivity"
            android:label="Basic Map" />

        <activity
            android:name=".CurrentLocationActivity"
            android:label="Current Location" />

        <activity
            android:name=".ZoomLocationActivity"
            android:label="Zoom Location" />

        <activity
            android:name=".AddMarkerActivity"
            android:label="Add Marker" />

        <activity
            android:name=".AddPolylineActivity"
            android:label="Add Polyline" />

        <activity
            android:name=".AddPolygonActivity"
            android:label="Add Polygon" />

    </application>

</manifest>